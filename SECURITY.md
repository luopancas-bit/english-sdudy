# 安全说明

## 报告问题

请不要在公开 Issue、讨论区或 PR 中粘贴密码、AccessKey、Cookie、数据库、服务器地址、录音或课程原文。安全问题应通过仓库所有者的 GitHub 私密渠道报告。

## 凭据处理

- GitHub 仓库和 Docker 镜像不保存生产凭据。
- 生产密钥通过服务器环境文件或秘密管理服务注入。
- 误公开的密钥视为已经泄露：立即禁用或轮换，再清理 Git 历史。
- 示例账号、测试令牌和邀请码必须是不可用于生产的合成值。

## 个人数据

账号资料、答题记录、错题、会话和跟读录音属于个人数据。数据库与录音目录必须使用独立私有卷，不允许打包进镜像或提交到 Git。

## 依赖与提交

CI 会执行密钥扫描、类型检查、测试和生产构建。远程协作者不得绕过这些检查，也不得通过修改 `.gitignore` 把受限文件纳入版本控制。
