# 远程协作说明

## 开始前

1. 从 `main` 创建 `agent/<主题>` 分支。
2. 只使用 `.env.example` 创建本地配置。
3. 测试课程使用合成数据；不要索取或复制生产课程、账号数据库和录音。

## 提交要求

- 掌握率只能由计分考核和计划复习改变。
- 修改权重、档位、复习周期或“同日首次成绩”规则时，必须同步修改领域测试和文档。
- 修改数据库结构时，必须包含向前迁移和恢复说明。
- 不允许把真实 IP、域名运维细节、密码、AccessKey、Cookie 或证书写进代码、Issue、日志样例和截图。
- 提交前运行 `pnpm check`。

## Pull Request

PR 需要说明：

- 为什么改；
- 对学习规则和已有数据有何影响；
- 电脑与 iPhone 两种布局如何验证；
- 是否新增网络权限、数据收集或私有内容依赖。

远程协作者只通过 PR 提交，不直接操作生产服务器。生产部署、私有课程挂载、数据库迁移和旧系统下线由仓库所有者单独授权。
